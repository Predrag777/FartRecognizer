# FartRecognizer

Run this command in your environemnt to install neccessary libraries: **pip install sounddevice numpy tensorflow librosa soundfile**

To test model run testScript and try to imitate farts.
